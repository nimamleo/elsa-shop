{
  "name": "shop",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "workspaces": [
    "api-gateway"
  ],
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "npm run prebuild --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "start": "nest run start --workspaces --if-present"
  }
}
